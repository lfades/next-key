# Checks for authentication
directive @auth(
  # if true no error will be thrown if there's an error with the accessToken
  required: Boolean,
  # Checks for a specific scope in the accessToken
  scope: [String],
) on FIELD_DEFINITION
